class Vehicle {
    constructor(name, model, color1, color2) {
        this.name = name;
        this.model = model;
        this.color1 = color1;
        this.color2 = color2;
    }

    getDetails() {
        return Name; {this.name}nModel:{this.model}nColor:{this.weight}nColor: {this.color};
        let vehicle1 = new Vehicle("Honda", "Mobilio", "Undefined", "White");
        let vehicle2 = new Vehicle("Toyota", "Fortuner", "Undefined", "Blue");
        
        console.log("Vehicle -1 Details:");
        console.log(vehicle1.getDetails());
        
        console.log("\nVehicle -2 Details:");
        console.log(vehicle2.getDetails());  
}
}
